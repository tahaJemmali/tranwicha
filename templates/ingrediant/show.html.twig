{% extends 'user/admin/back.html.twig' %}

{% block title %}Ingrediant{% endblock %}

{% block body %}
    <h1>Ingrediant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingrediant.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ingrediant.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ingrediant.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ingrediant_index') }}">back to list</a>

    <a href="{{ path('ingrediant_edit', {'id': ingrediant.id}) }}">edit</a>

    {{ include('ingrediant/_delete_form.html.twig') }}
{% endblock %}
